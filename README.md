# Zazerkal Repertoire Toolkit

Инструментарий для автоматизации афиши Театра «Зазеркалье». Проект состоит из PHP‑админки (управление спектаклями, карточками, выгрузкой во VK) и Node.js‑скрапера, который снимает актуальные составы из Temza и сохраняет их в JSON.

## Структура репозитория

```
.
├── theater_repertoire/   # веб‑приложение (PHP + MySQL)
│   ├── temza.php         # сопоставление Temza ↔ афиша, запуск скрапера/импортера
│   ├── schedule.php      # статусы готовности карточек
│   ├── template_editor.php, ... (редактор шаблонов ролей)
│   └── scripts/import_temza_json.php
├── temza_scraper/        # Puppeteer скрапер Temza → JSON
│   └── output/           # temza-YYYY-MM.json (игнорируется Git)
├── migrations/           # SQL‑патчи
└── README.md             # этот файл
```

## Требования

| Компонент | Версия |
|-----------|--------|
| PHP       | 8.1+   |
| MySQL/MariaDB | 10.4+ |
| Node.js   | 18+ (для Puppeteer) |
| Chromium dependencies | `libnspr4`, `libnss3`, `libgtk-3`, `libgbm1`, … (см. [Puppeteer troubleshooting](https://pptr.dev/troubleshooting)) |

## Конфигурация

### `theater_repertoire/config.php`

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'avo_zaze');
define('DB_USER', 'root');
define('DB_PASS', '***');

define('PROJECT_ROOT', realpath(__DIR__ . '/../'));
define('THEATER_APP_DIR', __DIR__);
define('TEMZA_SCRAPER_DIR', PROJECT_ROOT . '/temza_scraper');
define('TEMZA_OUTPUT_DIR', TEMZA_SCRAPER_DIR . '/output');
define('PHP_CLI_BINARY', '/usr/bin/php'); // при необходимости поменять
```

На сервере достаточно поправить пути/учётные данные. В VK‑настройках используется `vk_config.php` (в `.gitignore`), доступы хранятся в БД (`system_settings` → `vk_access_token`).

### `temza_scraper/.env`

```env
TEMZA_LOGIN_URL=https://system.temza.online
TEMZA_USERNAME=ulogin
TEMZA_PASSWORD=upassword
TEMZA_MONTH_URL_TEMPLATE=https://system.temza.online/month/{year}/{month}
PUPPETEER_HEADLESS=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser   # если не нужен встроенный
```

## Развёртывание

1. **Клонировать репозиторий** на сервер, оставить структуру как локально (`theater_repertoire` и `temza_scraper` рядом).
2. **База данных** — применить `migrations/*.sql` или загрузить последнюю дамп‑копию. Следить, чтобы таблицы `temza_events`, `temza_cast_resolved`, `temza_change_log` и `template_elements.special_group` были в схеме.
3. **PHP** — настроить виртуальный хост на `theater_repertoire/`. При необходимости обновить `config.php`.
4. **Node.js**:
   ```bash
   cd temza_scraper
   npm install
   npx puppeteer browsers install chrome   # один раз, скачивает Chromium
   ```
5. Убедиться, что у веб‑пользователя есть права на `temza_scraper/output/` и на выполнение `/usr/bin/php`.

## Работа со скрапером

CLI:

```bash
cd temza_scraper
npm run dev -- --months=2025-11,2025-12
```

Параметры:

* `--months=current,next` (по умолчанию) или явные `YYYY-MM`.
* `--month=YYYY-MM` — одиночный месяц.
* `--outDir=/path/to/output`.

Через UI (`temza.php` → «Сбор данных из Темзы»):
1. Ввести список месяцев.
2. Нажать «Запустить скрапер». PHP запустит Puppeteer внутри указанной папки.
3. Ниже появится таблица с датами выгрузок и логом.

## Импорт JSON → БД

**Автоматический способ:** всё на той же странице `temza.php`.

1. Блок «Импорт JSON → база данных».
2. Указать список месяцев (после успешного скрапа).
3. Нажать «Импортировать в базу». Скрипт `scripts/import_temza_json.php`:
   * очищает `temza_events` выбранного месяца;
   * загружает новый JSON;
   * восстанавливает сопоставления (`matched_event_id`, `ignore`, `published_at`);
   * пересобирает `temza_cast_resolved` и фиксирует изменения в `temza_change_log`.

После импорта статусы карточек обновляются.

## Основные страницы админки

| Страница | Описание |
|----------|----------|
| `/schedule.php` | Показывает календарь и статус состава: `Заполнено`, `Изменено` (есть изменения после отправки), `Требует подтверждения` (есть неподтверждённые роли). Также отображает отметку «Опубликовано». |
| `/temza.php` | Главный интерфейс Temza: сопоставление названий, событий, запуск скрапера/импортера, просмотр карточек перед публикацией в VK («Отправить в VK» / «Снять отметку»). |
| `/temza_roles.php` | Маппинг ролей Temza → шаблон карточки. Поддерживает групповые роли, игнорирование, флажок «Брать предыдущий состав», статус «Пересобрать составы». |
| `/template_editor.php` | Редактор шаблонов карточек: заголовки, роли, флажок «Берём предыдущий состав», привязка к спец‑группам (дирижёр, концертмейстер), добавление ссылки на билеты. |

## VK интеграция

* Токены хранятся в `system_settings` (`vk_access_token`). После любых утечек нужно генерировать новый и обновлять записи.
* Публикация карточки в VK ведётся через `publish_to_vk.php` (кнопка на `temza.php`). Статус хранится в `temza_events.published_at/published_by`.

## Примечания по деплою

* **Пути:** если структура на сервере отличается, достаточно поправить константы в `config.php`.
* **Puppeteer:** при ошибке `libnspr4.so: cannot open shared object file` установите системные зависимости (`apt install libnspr4 libnss3 libgtk-3-0 ...`).
* **Безопасность:** `.env`, `vk_config.php`, SQL‑дампы и прочие чувствительные файлы добавлены в `.gitignore`. Не забудьте удалить секреты из истории, если они попадали в Git, и сменить токены.

## История изменений (коротко)

* **Статусы на schedule** — теперь учитывают Temza‑касты, изменения и публикацию.
* **Карточки Temza** — поддержка спец‑групп (дирижёр/пианист), кнопка пересборки составов, сохранение меток «Опубликовано».
* **Пути и PHP CLI** — вынесены в конфиг; `temza.php` больше не зависит от относительного положения каталогов.
* **UI** — добавлены блоки запуска скрапера и импорта с логами, фильтры запоминают выбранный месяц.

С полным списком задач можно познакомиться в `memory-bank/temza_integration_plan.md`.

---

Для подробностей по скраперу (структура `src/`, параметры CLI) см. `temza_scraper/README.md`. Если заметили ошибку или нужна новая автоматизация — создайте issue или свяжитесь с maintainer'ом. Удачной выгрузки! 🎭

